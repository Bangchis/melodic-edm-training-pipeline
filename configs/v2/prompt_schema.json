{
  "schema_version": "2.0",
  "training_prompts": [
    {
      "index": 0,
      "type": "canonical",
      "purpose": "whole-track summary",
      "word_range": [40, 80]
    }
  ],
  "auxiliary_annotation_views_not_embedded": ["composition", "production"],
  "training": {
    "audio_latents_per_record": 1,
    "prompt_embeddings_per_record": 1,
    "selection": "single fused canonical prompt on every load",
    "cfg_dropout": 0.15
  },
  "validation": {
    "selection": "canonical index 0 only",
    "cfg_dropout": 0.0
  },
  "separate_fields": ["bpm", "keyscale", "timesignature", "sections"]
}
